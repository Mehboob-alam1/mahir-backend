package com.example.demoapp.entity;

public enum Role {
    USER,   // Find Mahir (customer)
    MAHIR,  // Become Mahir (professional)
    ADMIN   // Administrator (can authenticate and manage app)
}
